/* This file was automatically generated using [Sqlite3pp_EZ].
Sqlite3pp_EZ Copyright (C) 2025 David Maisonave (http::\www.axter.com)
Example Usage:		(Using sqlite3pp::Table container)
	// Example #1
		sqlite3pp::setGlobalDB("myDatabase.db");
		sqlite3pp::Table< SQL_<Database_Filename>_<table_view>_<Table_View_Name>> my_tbl;
		for (auto& row : my_tbl)
			std::cout << row << std::endl;

	// Example #2
		for (int i = 0; i < my_tbl.size(); ++i)
			std::cout 
					<< my_tbl[i].get_<Table_Column_Name>() 
					<< std::endl;

	// Example #3
		for (auto r = my_tbl.begin(); r != my_tbl.end(); ++r)
			std::cout 
					<< my_tbl[i].get_<Table_Column_Name>() 
					<< std::endl;

	// Example #4
		sqlite3pp::setGlobalDB("myDatabase.db");
		sqlite3pp::Table<SQL_<Database_Filename>_<table_view>_<Table_View_Name>> my_tbl;
		// Example#4a -- (C++11) Range-based loop
		for(auto& row : my_tbl)
			std::cout 
				<< row.get_<Table_Column_Name>() << " | " 
				<< std::endl;

		// Example#4b -- C++ style iteration
		for (auto row = my_tbl.begin(); row != my_tbl.end(); ++row) 
			std::cout 
				<< row->get_<Table_Column_Name>() << " | " 
				<< std::endl;

		// Example#4c -- C style iteration
		for (int row = 0; row < my_tbl.size(); ++row) 
			std::cout 
				<< my_tbl[row].get_<Table_Column_Name>() << " | " 
				<< std::endl;
For more details see  https://github.com/David-Maisonave/sqlite3pp_EZ
*/
#ifndef SQL_<DATABASE_FILENAME>_<TABLE_VIEW>_<TABLE_VIEW_NAME>_H
#define SQL_<DATABASE_FILENAME>_<TABLE_VIEW>_<TABLE_VIEW_NAME>_H
#include <string>
#include "..\sqlite3pp_EZ\sqlite3pp_ez.h"

class SQL_<Database_Filename>_<table_view>_<Table_View_Name>: public sqlite3pp::sql_base
{
	// A member variable for each field in the <table_view>
protected:
	<Table_Column_Type> <Table_Column_Name> = <Table_Column_Initial_Value>;

public:
	using StrType = std::string;
	// Constructors
	SQL_<Database_Filename>_<table_view>_<Table_View_Name>() {} // Default constructor
	template <class T> SQL_<Database_Filename>_<table_view>_<Table_View_Name>(const T &t) { setData(t); } // Allows data input from different (or same) tables/views having the same data types and column names

	// getTableName, getColumnNames, getSelectColumnNames, and getStreamData are required for sqlite3pp::Table template class
	static StrType getTableName() { return "<Table_View_Name>" ; } // Get table/view name
	static StrType getColumnNames() 
	{ 
		return 
				"\"<Table_Column_Name>\"<GetColumnNameDelimiter> "
				; 
	}
	static StrType getSelectColumnNames()
	{ 
		return 
				"\"<Table_Column_Name>\"<GetSelectColumnNamesDelimiter> "
				; 
	}
	StrType GetValues() const
	{
		StrType strtype;
		strtype +=  "'" + sqlite3pp::to_string( <Table_Column_Name>) + "'";
		strtype +=  ",'" + sqlite3pp::to_string( CCglobal) + "'";
		return strtype;
	}
	template<class T> void getStreamData( T q ) { q.getter()  >> <Table_Column_Name> >> CCglobal;}
	template <class T> void setData(const T &t) // Used to transfer data from different tables/views having same data types and column names
	{
		<Table_Column_Name> = t.get_<Table_Column_Name>();
		CCglobal = t.get_CCglobal();
	}

	// Miscellaneous functions
	static int getColumnCount() { return 2; }

	// A get_ function for each field in the <table_view>, which allows read-only access to protected member variables.
	const <Table_Column_Type>& get_<Table_Column_Name>() const {return <Table_Column_Name>;}
	const Real& get_CCglobal() const {return CCglobal;}

	// A set_ function for each field in the <table_view>.
	void set_<Table_Column_Name>(const <Table_Column_Type>& data__) {<Table_Column_Name> = data__;}
	void set_CCglobal(const Real& data__) {CCglobal = data__;}

	////////////////////////////////////////////////////////////////////////////////////////////
	/* sqlite3pp::TableOStream container interface.
	Functions OStream(), operator<<(), and Delimiter() are required when using the sqlite3pp::TableOStream container.
	Example Usage:		(Using sqlite3pp::TableOStream container)
			TableOStream<SQL_<Database_Filename>_<table_view>_<Table_View_Name>> tbl(DbFileNameArg("myDatabase.db"));
			tbl.setDelimit("|"); // Change delimiter
			std::cout << tbl; // Send data to screen with the changed delimiter

			std::ofstream ofs ("data.csv", std::ofstream::out);
			tbl.setDelimit(","); // Change delimiter
			ofs << tbl; // Write data to a CSV file using the changed "," delimiter.

			tbl.out(std::cout); // Send data to screen using out() member function.
	To exclude TableOStream interface, set exclude_ostream_operator to true when creating this class using SQLiteClassBuilder.
	*/
	template<class T> T& OStream(T& t) const
	{
		t.os << <Table_Column_Name> << t.d << CCglobal;
		return t;
	}
	friend std::ostream& operator<<(std::ostream& os, const SQL_<Database_Filename>_<table_view>_<Table_View_Name>& t);
	friend std::wostream& operator<<(std::wostream& os, const SQL_<Database_Filename>_<table_view>_<Table_View_Name>& t);
	static StrType Delimiter() { return "," ; }
	////////////////////////////////////////////////////////////////////////////////////////////
};
// sqlite3pp::TableOStream container interface.
inline std::ostream& operator<<(std::ostream& os, const SQL_<Database_Filename>_<table_view>_<Table_View_Name>& t) { sqlite3pp::ostream_a o(os, t.Delimiter()); return t.OStream(o).os; }
inline std::wostream& operator<<(std::wostream& os, const SQL_<Database_Filename>_<table_view>_<Table_View_Name>& t) { sqlite3pp::ostream_w o(os, t.Delimiter());  return t.OStream(o).os; }

#endif // !SQL_<DATABASE_FILENAME>_<TABLE_VIEW>_<TABLE_VIEW_NAME>_H
